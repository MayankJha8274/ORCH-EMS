Frontend (Operator Dashboard)

Quick start:

1. cd frontend
2. npm install
3. npm run dev

The app expects the backend API to be available at `http://localhost:5000` by default. Set `VITE_API_URL` in a `.env` file if needed.
